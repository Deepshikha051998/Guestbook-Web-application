apiVersion: v1
kind: Service
metadata:
  name: gbfrontend
  labels:
    app.kubernetes.io/name: gbfrontend
    app.kubernetes.io/component: frontend
spec:

  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app.kubernetes.io/name: gbfrontend
    app.kubernetes.io/component: frontend